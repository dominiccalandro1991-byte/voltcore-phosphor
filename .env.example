# VOLTCORE PHOSPHOR — Vercel must have ZERO secrets.
# Dual-Rail keys live only on Cloudflare Worker core-api:
#   SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, OPENROUTER_API_KEY,
#   MESH_HMAC, XAI_API_KEY, GITHUB_TOKEN, MONDAY_SIGNING_SECRET
#
# Do not copy any of those into Vercel Environment Variables.
